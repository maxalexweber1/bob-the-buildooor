# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
*.crx
*.zip
*.pem

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/
Thumbs.db

# Test / coverage
coverage/
.nyc_output/

# Dev / proof tooling — not shipped (icon generator + testnet experiment scripts).
# The generated product assets (src/assets/icon-*.png) ARE committed; the generator is not.
scripts/
_logo_src.png

# Local PR drafts — not part of the committed docs.
docs/PR-*.md

# Local review working doc — findings are resolved in-code; not part of the committed docs.
docs/WALLET_REVIEW_FINDINGS.md

# Local agent instructions (Codex etc.) — not part of the committed repo.
AGENTS.md

# Playwright e2e artifacts
test-results/
playwright-report/

# CIP-113 local deployment artifact (contains preview deployment refs)
scripts/cip113-params.local.json
